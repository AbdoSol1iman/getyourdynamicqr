# Dependencies
node_modules/

# Environment variables (never commit secrets)
.env
.env.local

# Prisma generated client
generated/

# Logs
*.log

# OS files
.DS_Store

# Agent skills / tooling (local machine only, not part of the app)
.agents/
skills-lock.json

# Legacy root vercel.json (monorepo services config) — the frontend deploys
# through the Vercel git integration rooted at /frontend, so this is unused.
/vercel.json
